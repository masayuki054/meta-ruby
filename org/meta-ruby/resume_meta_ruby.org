* メタプログラミング Ruby のまとめのまとめ

** お断り

*** 「メタプログラミング Ruby のまとめ」のまとめ

    講義で短かく説明するために，まとめのまとめを作りました。

*** 記法 
   
     もともとは RubyDocument (RD) で書いた文書を Emacs Org-mode にしまし
     た。RDが優れている記法はそのまま残してあります。

     ((...)) の箇所です。(({...})) Rubyソース・コードです。

*** ソースコードへのリンク

この文書は，Emacs Org-mode で読んでもらおうと思っています。この文書中
コピーし，ソースコードへのリンク中の "file:~/" を "file://~suzuki/" に
変更してください。

#+include: resume_object_model.org

